import TransactionForm from "@/components/TransactionForm";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <TransactionForm />
    </div>
  );
};

export default page;
